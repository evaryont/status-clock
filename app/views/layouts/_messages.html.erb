<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
  <div data-alert="" class="alert-box radius flash-<%= name %>">
      <%= msg %>
      <a href="" class="close">×</a>
  </div>
  <% end %>
<% end %>
